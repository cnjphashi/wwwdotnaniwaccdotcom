---
import { features } from '../data/siteData';
---

<div id="why-choose-us">
<section id="features" class="py-24 bg-bg-secondary">
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
    <div class="text-center mb-20">
      <h2 class="text-2xl md:text-3xl font-medium text-primary mb-4"> 選ばれる理由</h2>
    </div>

    <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-8">
      {features.map((feature) => (
        <div class="bg-bg-primary p-8 rounded-xl border border-border-white hover:border-primary transition-all transform hover:-translate-y-[2px]">
          <div class="w-16 h-16 bg-primary/10 rounded-lg flex items-center justify-center mb-6">
            <i class={`${feature.icon} text-4xl text-primary`}></i>
          </div>
          <h3 class="text-lg  text-text-secondary mb-3">{feature.title}</h3>
          <p class="text-gray-400 leading-relaxed text-text-primary">{feature.description}</p>
        </div>
      ))}
    </div>
  </div>
</section>
</div>
